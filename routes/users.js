const { wrap } = require('async-middleware');

// const FieldController = require('../controllers/field');
// const ProfileController = require('../controllers/profile');

module.exports = router => {
  // router.put(
  //     '/field/:id',
  //     wrap(FieldController.validateUpdateRequest),
  //     ProfileController.assignLastUpdatedBy,
  //     wrap(FieldController.update)
  // );
  // router.delete('/field/:id', wrap(FieldController.delete));
};
